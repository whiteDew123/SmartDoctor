<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.medical.mapper.DrugMapper">

    <resultMap id="BaseResultMap" type="com.qst.medical.entity.Drug">
        <id column="drug_id" property="drugId"/>
        <result column="drug_name" property="drugName"/>
        <result column="drug_info" property="drugInfo"/>
        <result column="drug_effect" property="drugEffect"/>
        <result column="drug_img" property="drugImg"/>
        <result column="publisher" property="publisher"/>
        <result column="createtime" property="createtime"/>
        <result column="updatetime" property="updatetime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM drug
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM drug WHERE drug_id = #{drugId}
    </select>

    <insert id="insert" parameterType="com.qst.medical.entity.Drug" useGeneratedKeys="true" keyProperty="drugId">
        INSERT INTO drug (drug_name, drug_info, drug_effect, drug_img, publisher, createtime, updatetime)
        VALUES (#{drugName}, #{drugInfo}, #{drugEffect}, #{drugImg}, #{publisher}, NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.qst.medical.entity.Drug">
        UPDATE drug
        SET drug_name = #{drugName},
            drug_info = #{drugInfo},
            drug_effect = #{drugEffect},
            drug_img = #{drugImg},
            publisher = #{publisher},
            updatetime = NOW()
        WHERE drug_id = #{drugId}
    </update>

    <delete id="deleteById">
        DELETE FROM drug WHERE drug_id = #{drugId}
    </delete>

    <select id="countAll" resultType="java.lang.Long">
        SELECT COUNT(*) FROM drug
    </select>

</mapper>
