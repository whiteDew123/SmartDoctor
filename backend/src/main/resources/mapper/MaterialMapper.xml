<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.medical.mapper.MaterialMapper">

    <resultMap id="BaseResultMap" type="com.qst.medical.entity.Material">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="message" property="message"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM material
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM material WHERE id = #{id}
    </select>

    <select id="selectByKeyword" resultMap="BaseResultMap">
        SELECT * FROM material 
        WHERE title LIKE CONCAT('%', #{keyword}, '%') 
           OR message LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <insert id="insert" parameterType="com.qst.medical.entity.Material" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO material (title, message, create_time, update_time)
        VALUES (#{title}, #{message}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.qst.medical.entity.Material">
        UPDATE material 
        SET title = #{title}, 
            message = #{message}, 
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM material WHERE id = #{id}
    </delete>

</mapper>